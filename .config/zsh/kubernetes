# vim:ft=zsh

compdef k=kubectl
compdef kubecolor=kubectl

alias k='kubecolor --force-colors'
alias kgp='kubectl get pods'
alias kdp='kubectl describe pods'
alias kgd='kubectl get deployment'
alias kdd='kubectl describe deployment'
alias kgsts='kubectl get statefulset'
alias kdsts='kubectl describe statefulset'
alias kgcm='kubectl get configmaps'
alias kgsec='kubectl get secret'
alias kgns='kubectl get namespace'
alias kcn='kubectl config set-context --current --namespace'
alias kcu='kubectl config get-contexts --output name | fzf | xargs -r -I {} kubectl config use-context {}'

# Trailing space expands the following alias (e.g. `watch kgp`); keep it.
alias watch='KUBECOLOR_FORCE_COLORS=true watch --color '

# k8s
export KUBE_EDITOR=nvim
# Use ~/.kube/kuberc for kubectl config
export KUBECTL_KUBERC=true

# kubectl krew for kubectl plugin management
[[ -d "${HOME}/.krew/bin" ]] && export PATH="${HOME}/.krew/bin:${PATH}"

# alias kcu='kubectl config get-contexts -o name | fzf | xargs -r -I {} kubectl config use-context "{}"'
function kgsecv {
  kubectl get secret "${1}" --output yaml | yq '.data | map_values(@base64d)'
}

