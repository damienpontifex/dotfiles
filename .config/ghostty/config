# The template does not set any default options, since Ghostty ships
# with sensible defaults for all options. Users should only need to set
# options that they want to change from the default.
#
# Run `ghostty +show-config --default --docs` to view a list of
# all available config options and their default values.
#
# Additionally, each config option is also explained in detail
# on Ghostty's website, at https://ghostty.org/docs/config.
#
# Config syntax crash course
# ==========================
# # The config file consists of simple key-value pairs,
# # separated by equals signs.
# font-family = Iosevka
# window-padding-x = 2
#
# # Empty values are used to reset config keys to default.
# key =
#
theme = nord

command = "/bin/zsh -lc 'tmux attach || tmux new-session -s pontifex'"

macos-option-as-alt = true
copy-on-select = clipboard
clipboard-read = allow
clipboard-write = allow

font-family = "JetBrainsMonoNL Nerd Font Mono"
font-size = 14
adjust-cell-height = +17%
font-thicken = false
macos-titlebar-style = hidden
window-save-state = never
confirm-close-surface = false
quit-after-last-window-closed = true
window-padding-x = 0
window-padding-y = 0

cursor-style = block
cursor-style-blink = false
mouse-hide-while-typing = true

# Example Split Navigation
# keybind = ctrl+alt+left=goto_split:left
# keybind = ctrl+alt+right=goto_split:right
# keybind = ctrl+alt+up=goto_split:top
# keybind = ctrl+alt+down=goto_split:bottom
# 
# # Example New Splits
# keybind = alt+\=new_split:right
# keybind = alt+-=new_split:down
# 
# # Alt + l/h for cycling through tabs (Ghostty "windows" are generally tabs in iTerm/tmux)
# keybind = alt+l=next_tab
# keybind = alt+h=previous_tab
# 
# keybind = ctrl+a>l=goto_split:right
# keybind = ctrl+a>h=goto_split:left
# keybind = ctrl+a>k=goto_split:top
# keybind = ctrl+a>j=goto_split:bottom
# 
# keybind = ctrl+a>|=new_split:right
# keybind = ctrl+a>-=new_split:down
# keybind = ctrl+a>z=toggle_split_zoom
# keybind = ctrl+a>r=reload_config
# 
# keybind = ctrl+a>c=new_tab
# keybind = ctrl+a>n=next_tab
# keybind = ctrl+a>p=previous_tab
# # Ctrl+Shift+Left/Right to move tab left/right and select it
# # keybind = ctrl+shift+left=swap_tab:left
# # keybind = ctrl+shift+right=swap_tab:right
# 
# keybind = ctrl+a>s=toggle_tab_overview
